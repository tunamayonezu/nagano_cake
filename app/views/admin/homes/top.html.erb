<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-8 offset-md-1'>
      <h2>注文履歴一覧</h2>
      <%= link_to "ログアウト", destroy_admin_session_path, method: :delete %>
      <table class='table table-hover table-inverse'>
        <thead>
          <tr>
            <th>購入日時</th>
            <th>購入者</th>
            <th>注文個数</th>
            <th>注文ステータス</th>
          </tr>
        </thead>
        <%# books.each do |book| %>
         <!--多分オーダー？-->
        <%# @orders.each do |order| %>
         <tbody>
           <tr>
             <td><%#= link_to admin_order_path %><%#= @order.created_at.strftime('%Y/%m/%d') %></td>
             <td><%#= @order.customer.name %></td>
             <td></td>
             <td></td>
           </tr>
         </tbody>
      </table>
      <%= paginate @orders%>
    </div>
  </div>
</div>